import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.UniversidadaUpap.testOneSignalNotifications',
  appName: 'pushapp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  },
};

export default config;
